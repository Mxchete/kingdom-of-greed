using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Greyson : Entity
{

    protected override void Awake()
    {
        base.Awake(); // Call the base class's Update method if needed
        // No movement logic, so the NPC stays still
    }
}
